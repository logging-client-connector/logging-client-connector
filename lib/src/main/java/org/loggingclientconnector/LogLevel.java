package org.loggingclientconnector;

/**
 * Represents the log levels used in the LoggingClientConnector.
 * <p>
 * Example usage:
 * <pre>{@code
 * loggerConfig.logLevel(LogLevel.INFO);
 * }</pre>
 */
public enum LogLevel {
	TRACE,
	DEBUG,
	INFO,
	WARN,
	ERROR
}
